name: Frontend CI/CD

on:
  push:
    branches:
      - master
      - main
    paths:
      - 'RuoYi-SpringBoot3-ElementPlus-master/**'
      - '.github/workflows/frontend.yml'
  pull_request:
    branches:
      - master
      - main
    paths:
      - 'RuoYi-SpringBoot3-ElementPlus-master/**'
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  # ===== 前端代码检查 & 构建 =====
  frontend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: RuoYi-SpringBoot3-ElementPlus-master

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: RuoYi-SpringBoot3-ElementPlus-master/package-lock.json

      - name: Install dependencies
        run: npm ci --prefer-offline

      - name: Lint check
        run: npm run lint || true

      - name: Build production
        run: npm run build:prod

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist-${{ github.run_id }}
          path: RuoYi-SpringBoot3-ElementPlus-master/dist/
          retention-days: 7

  # ===== Docker 镜像构建 =====
  docker-build:
    needs: frontend-ci
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      DOCKER_REGISTRY: ${{ secrets.DOCKER_REGISTRY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Check Docker publish secrets
        id: publish
        shell: bash
        run: |
          if [ -n "$DOCKER_USERNAME" ] && [ -n "$DOCKER_PASSWORD" ] && [ -n "$DOCKER_REGISTRY" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Login to Docker Hub
        if: steps.publish.outputs.enabled == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}

      - name: Build Docker image
        if: steps.publish.outputs.enabled != 'true'
        run: |
          docker build \
            -t smart-parking-ui:ci \
            -f ./RuoYi-SpringBoot3-ElementPlus-master/Dockerfile.nginx \
            ./RuoYi-SpringBoot3-ElementPlus-master

      - name: Extract metadata
        if: steps.publish.outputs.enabled == 'true'
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_REGISTRY }}/smart-parking-ui
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        if: steps.publish.outputs.enabled == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./RuoYi-SpringBoot3-ElementPlus-master
          file: ./RuoYi-SpringBoot3-ElementPlus-master/Dockerfile.nginx
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===== 部署到服务器 =====
  deploy:
    needs: docker-build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
    environment: production
    env:
      SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
      SERVER_HOST: ${{ secrets.SERVER_HOST }}
      SERVER_USER: ${{ secrets.SERVER_USER }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check deploy secrets
        id: deploy
        shell: bash
        run: |
          if [ -n "$SERVER_SSH_KEY" ] && [ -n "$SERVER_HOST" ] && [ -n "$SERVER_USER" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup SSH key
        if: steps.deploy.outputs.enabled == 'true'
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ env.SERVER_SSH_KEY }}

      - name: Deploy to server
        if: steps.deploy.outputs.enabled == 'true'
        run: |
          ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST << 'EOF'
            cd /opt/smart-parking
            docker compose pull ruoyi-ui
            docker compose up -d ruoyi-ui
            docker image prune -f
          EOF

      - name: Smoke test
        if: steps.deploy.outputs.enabled == 'true'
        run: |
          curl -sf http://${{ secrets.SERVER_HOST }}:${{ secrets.UI_PORT || 80 }}/health || exit 1
          echo "Frontend deployment verified"

      - name: Skip deployment
        if: steps.deploy.outputs.enabled != 'true'
        run: echo "Deployment secrets are not configured; skipping deployment."
