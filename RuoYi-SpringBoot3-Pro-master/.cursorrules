> Always respond in 中文

## 回答风格
- 直接、务实、少空话。先给可执行结论，再补充原因。
- 涉及 Java/Spring Boot 配置、依赖、构建问题时，优先给 Maven/Gradle 命令。

## 技术栈
- Spring Boot 3 + RuoYi-Vue-Pro 框架
- MySQL (finpilot_dev) + Kafka
- Lombok 减少样板代码

## Java 编码规范
- 使用 Lombok（@Data, @Slf4j 等），不写重复 getter/setter
- Service 层禁止硬编码 Kafka Bootstrap Servers，从 `@ConfigurationProperties` 注入
- 新增 Controller 要在模块的 Router 注册
- 测试放在 `ruoyi-biz/src/test/` 目录
- REST 统一返回格式使用 RuoYi 的 `R` 类

## 构建与运行
```powershell
# 本地启动（确保 MySQL 和 Kafka 已就绪）
cd RuoYi-SpringBoot3-Pro-master
mvn spring-boot:run -Dspring-boot.run.profiles=docker

# Docker 构建
docker build -t smart-parking-backend .
docker compose up -d

# 验证健康状态
curl http://localhost:8080/api/v1/health
```

## 配置文件
- 开发配置：`src/main/resources/application-druid.yml`
- Docker 配置：`src/main/resources/application-docker.yml`
- 参考 `application-secure-template.yml` 管理敏感字段

## Kafka 消费者
- Topic：`ai-event-topic`
- 消费者组：`parking-group`
- `KafkaConsumerService` 消费消息，转发给 `AiAlarmOrchestrator`
- 告警去重 + 限流逻辑在 `AiAlarmOrchestrator`

## 禁止事项
- 不要在代码中硬编码数据库连接串和密码
- 不要提交 pom.xml 中的本地路径依赖
- 不要在生产环境开启 debug 日志级别
