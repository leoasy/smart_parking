> Always respond in 中文

## 回答风格
- 直接、务实、少空话。先给可执行结论，再补充原因。
- 涉及 Python/FastAPI 依赖、部署、推理问题时，优先给可运行的命令。

## 技术栈
- FastAPI + YOLO (parking.pt)
- Python 3.x + uvicorn

## Python AI 服务规范
- 使用 type hints 标注所有函数参数和返回值
- 依赖声明在 `requirements.txt`，优先使用国内镜像安装
- API 路由放在 `app/api.py`，数据模型放在 `app/schemas.py`

## 启动与测试
```powershell
# 进入 Python 服务目录
cd Parking_Ai

# 安装依赖（使用国内镜像）
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 启动 FastAPI 推理服务（端口 8000）
python -m uvicorn main_api:app --reload --host 0.0.0.0 --port 8000

# 单张图片推理测试
python main.py --img ./data/test.jpg --parking-lot-id PL01 --camera-id 1

# 健康检查
curl http://localhost:8000/health
```

## 环境变量
- 参考 `.env.example`，不将 `.env` 提交到 GitHub
- 模型路径、阈值、相机配置等通过环境变量注入

## 模型管理
- 模型文件 `parking.pt` 禁止提交到 Git（已在 .gitignore）
- 首次运行自动下载模型到 `model/` 目录

## Docker 部署
```powershell
docker build -t parking-ai -f Dockerfile ..
docker compose up -d parking-ai
```

## 禁止事项
- 不要在代码中硬编码模型路径或密码
- 不要在生产环境开启 debug 模式
- 不要提交 .pt/.pth 等大模型文件
