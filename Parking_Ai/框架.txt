Parking_Ai/
├── app/                     # 对外接口层（FastAPI / HTTP / 后端）
│   ├── api.py               # 路由（/detect /status）
│   ├── deps.py              # 依赖注入（模型、engine）
│   └── schemas.py           # Pydantic 数据结构
│
├── core/                    # 核心业务层（不依赖具体模型）
│   ├── engine.py            # 🚗 停车位检测总调度引擎（核心）
│   ├── roi_loader.py        # ROI 加载（JSON → Python）
│   └── config.py            # 配置读取（yaml → dict）
│
├── inference/               # AI 推理层（可替换）
│   ├── detector.py          # YOLO 车辆检测
│   ├── roi_matcher.py       # 检测结果 ↔ ROI 匹配
│   └── stability.py         # 稳定性判断
│
├── utils/                   # 纯工具（无业务）
│   ├── draw.py              # 可视化
│   └── geometry.py          # 几何计算
│
├── tools/                   # 一次性 / 运维 / 标注工具
│   └── roi_labeler.py       # ROI 标注工具
│
├── data/                    # 数据（不写业务逻辑）
│   ├── images/
│   ├── roi/
│   └── outputs/
│
├── model/
│   └── parking.pt
│
├── config/
│   └── config.yaml
│
├── main.py                  # 🚀 单机调试入口（图片 / 视频）

