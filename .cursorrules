> Always respond in 中文

## 回答风格
- 直接、务实、少空话。先给可执行结论，再补充原因。
- 涉及配置、环境、报错排查时，优先给最可能成功的方案。
- 不确定我的环境时，明确假设条件，不要伪装成确定。

## 项目结构
smart-parking 是包含两个独立服务的 monorepo：
- `RuoYi-SpringBoot3-Pro-master/`：Spring Boot 3 Java 后端
- `Parking_Ai/`：FastAPI + YOLO Python AI 推理服务
- `docker-compose.yml`：统一启动脚本

## 架构要点
```
检测链路：
定时任务/相机 → RuoYi (ParkingDetectService)
    → HTTP POST /parking/detect/image → Parking_Ai (YOLO)
    → 更新 ParkingSlot → 写 AiEvent → 推 Kafka

告警链路：
Kafka (ai-event-topic) → KafkaConsumerService
    → AiAlarmOrchestrator (去重 + 限流) → 告警入库
```

## 禁止事项
- **不要自动生成文档**：不创建 README、设计文档、架构说明，除非用户明确要求
- **不要提交大文件**：*.pt 模型文件禁止提交
- **不要硬编码配置**：密码/API Key 等通过环境变量或 .env 管理

## 环境变量
- `.env` 含敏感信息，不提交 GitHub
- RuoYi 参考 `application-docker.yml` 和 `application-secure-template.yml`
- Parking_Ai 参考 `.env.example`

## Docker 开发
- 使用 `docker-compose.yml` 启动所有服务
- 挂载本地代码目录实现热重载

## Git 规范
- commit message：`type: subject`，type：feat / fix / refactor / chore / docs
- 推送 main 前确保可构建
- 大于 100MB 文件禁止提交

## 子模块规则
- Java 开发：见 `RuoYi-SpringBoot3-Pro-master/.cursorrules`
- Python AI：见 `Parking_Ai/.cursorrules`
